{
  "description": "Event description",
  "type": "object",
  "required": ["eventDate"],
  "properties": {
    "type": {
      "type": "string"
    },
    "eventDate": {
          "type": "string",
          "format": "date-time"
    },
    "investigationStart": {
          "type": "string",
          "format": "date-time"
    },
    "investigationEnd": {
          "type": "string",
          "format": "date-time"
    },
    "investigationReasons": {
      "type": "string"
    },
    "decision": {
      "type": "string"
    },
    "decisionDate": {
      "type": "string",
      "format": "date-time"
    }
  }
}