{
  "description": "Event description",
  "type": "object",
  "required": ["eventDate"],
  "properties": {
    "type": {
      "type": "string"
    },
    "eventDate": {
          "type": "string",
          "format": "date-time"
    },
    "decision": {
      "type": "string",
      "enum" : [
          "accept",
          "refuse",
          "retire",
          ""
      ]
    },
    "decisionDate": {
      "type": "string",
      "format": "date-time"
    }
  }
}